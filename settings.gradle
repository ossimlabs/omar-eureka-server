pluginManagement {
  repositories {
    maven { url "${ System.getenv('REPOSITORY_MANAGER_URL') }/ossim-deps" }
    gradlePluginPortal()
  }
}

include 'omar-eureka-server'

project(':omar-eureka-server').projectDir = "${rootProject.projectDir}/apps/omar-eureka-server" as File
